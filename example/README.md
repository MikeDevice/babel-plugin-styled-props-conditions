# Example for babel-plugin-styled-props-conditions

## Installation and run
1. Clone this repository.
2. Run follow commands:

```sh
$ npm install
$ nrun build

$ cd example
$ npm install
$ nrun dev
```
